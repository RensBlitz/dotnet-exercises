namespace Exercises;
public static class TinyEncryption
{
    /// <summary>Returns text shifted by offset (positive or negative, wrap A-Z / a-z).</summary>
    public static string ShiftCipher(string text, int offset)
        => throw new NotImplementedException();
} 