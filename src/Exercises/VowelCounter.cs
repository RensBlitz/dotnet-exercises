namespace Exercises;
using System.Collections.Generic;

public static class VowelCounter
{
    /// <summary>Returns map of vowels to their counts in sentence. a e i o u (case-insensitive).</summary>
    public static Dictionary<char, int> Count(string sentence)
        => throw new NotImplementedException();
} 